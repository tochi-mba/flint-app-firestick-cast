# An exact release rather than "stable", so a new toolchain cannot change the build of a commit that
# already passed. Moving it is a deliberate change, made in its own pull request.
[toolchain]
channel = "1.98.1"
targets = ["x86_64-pc-windows-msvc"]
components = ["clippy", "rustfmt"]
